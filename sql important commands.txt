SET SESSION sql_mode = '' - if any error in the dataset like (some extra comma/space) it will handle it.

load data infile 
'D:/sales_data_final.csv'
into table sales1 
fields terminated by ','
enclosed by '"'
lines terminated by '\n'
ignore 1 rows - It will help to dump the bulk data in the sql work bench

with the help of data we need to crate columns name automatically. we can do following steps.
install anacondaprompt
go to anaconda prompt
go to specific drive where the dataset is present.
pip install csvkit

csvsql --dialect mysql --snifflimit 100000 sales_data_final.csv > output_sales.sql
this command automatically Create mysql table directly from CSV file using the CSV Storage engine. 
